# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Change authorization

Do not modify, create, move, or delete files, install dependencies, run formatting or fix commands, or perform any other action that changes the project unless the user explicitly asks you to do so. When the user asks a question, only answer or diagnose it; do not implement a change unless implementation was directly requested.
